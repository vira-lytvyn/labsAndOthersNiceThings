function dx = f (t, x) //Функція, що описує систему дифрівнь першого порядку й має вхідними аргументами два параметри
    dx(1) = x(2); // перше рівняння системи дифрівнянь першого порядку
    dx(2) = -0.15*2*x(2)-x(1); // друге рівняння системи дифрівнянь першого порядку
endfunction
t = 0:0.01:30; // Задання проміжку інтегрування
y = ode([0; 5], 0, t, f); // Розвязання дифрівняння
plot2d(y(1,:), y(2, :)) // Побудова фазового портрету
